<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/sortBy"
        app:showAsAction="never"
        android:title="Sort By">
        <menu>
            <item android:id="@+id/sortByMissionName"
                app:showAsAction="never"
                android:title="Mission Name"/>
            <item android:id="@+id/sortByLaunchDate"
                app:showAsAction="never"
                android:title="Launch Date"/>
        </menu>
    </item>

    <item android:id="@+id/bookmarks"
        android:title="Show Bookmarks"
        android:icon="@drawable/ic_bookmarks"
        app:showAsAction="always"/>

    <item android:id="@+id/listViewType"
        android:title="Grid View"
        app:showAsAction="always"/>



</menu>